---
{% set name = env_prefix|lower + '-' + inventory_hostname|replace('_', '-')|lower %}
name: "{{ name }}"
region: "{{ new_region }}"
arn: "arn:aws:sns:{{ new_region }}:{{ aws_account }}:{{ name }}"
