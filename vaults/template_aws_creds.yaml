# The AWS authentication details used when deploying and destroying infrastructure.
# It's strongly recommended that this be encrypted with ansible vault
static_aws_access_key: ACCESS_KEY
static_aws_secret_key: SECRET_KEY
